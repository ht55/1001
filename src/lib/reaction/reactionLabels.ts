// src/lib/reaction/reactionAxisLabels.ts

import type { ReactionAxis } from "@/types/reactionProfile"

export const reactionAxisLabelMap: Record<ReactionAxis, string> = {
  resonance: "共鳴",
  tension: "緊張",
  distortion: "歪曲",
  collapse: "崩壊",
  void: "空虚",
  transcendence: "超越",
}
